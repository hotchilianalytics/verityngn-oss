# Minimal dependencies for Streamlit UI
# The UI only calls the API - it doesn't need AI/LangChain dependencies

# Web Framework
streamlit>=1.28.0

# API Communication
requests>=2.31.0

# Basic utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1

# YouTube Channel Selector Feature (v1.0.0-beta)
google-api-python-client>=2.157.0  # For YouTube Data API v3
yt-dlp>=2024.12.13  # Fallback for channel video fetching




