# Streamlit Secrets Configuration Template
# Copy this file to secrets.toml and fill in your values
# DO NOT commit secrets.toml to git (it's in .gitignore)

# Cloud Run API URL (REQUIRED for Cloud Run + Batch mode)
# This is the URL of your deployed Cloud Run service
CLOUDRUN_API_URL = "https://verityngn-api-ze7rxua3dq-uc.a.run.app"

# Optional: Local API URL (for development/testing)
# Uncomment and set if you want to use a local API server
# VERITYNGN_API_URL = "http://localhost:8080"

# Note: For Cloud Run mode, authentication is handled by the Cloud Run service
# No Google Cloud credentials needed here - the API handles authentication
