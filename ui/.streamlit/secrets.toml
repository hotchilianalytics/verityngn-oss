# Streamlit Secrets Configuration
# For Cloud Run mode testing - minimal configuration

# Cloud Run API URL (for Cloud Run + Batch mode)
CLOUDRUN_API_URL = "https://verityngn-api-ze7rxua3dq-uc.a.run.app"

# Optional: Local API URL (for local API testing)
# VERITYNGN_API_URL = "http://localhost:8080"

# Note: For Cloud Run mode, authentication is handled by the Cloud Run service
# No Google Cloud credentials needed here - the API handles authentication

